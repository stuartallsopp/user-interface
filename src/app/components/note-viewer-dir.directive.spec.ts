import { NoteViewerDirDirective } from './note-viewer-dir.directive';

describe('NoteViewerDirDirective', () => {
  it('should create an instance', () => {
    const directive = new NoteViewerDirDirective();
    expect(directive).toBeTruthy();
  });
});
